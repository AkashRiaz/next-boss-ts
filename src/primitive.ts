// Js -> string, number, boolean, undefined, null


//  TS => never, unknown, any, void

let userName: string = "Md akashuzzaman Riaz";

let userId = 123;

// userName.endsWith("Riaz");

console.log(userName.endsWith("Riaz"));

let isAdmin = true;

let isSuperAdmin: boolean = false;

let x: undefined = undefined;
